import { useLocale } from "@/hooks/useLocale"; import { GAME_PAGES } from "@/lib/gameTranslations"; import GameInfoPage from "@/components/GameInfoPage";
export default function CricketBetting() { const locale = useLocale(); const t = GAME_PAGES["cricket-betting"][locale]; return <GameInfoPage title={t.title} description={t.description} keywords="tekkabuzz cricket betting, ক্রিকেট বেটিং, IPL betting" canonical="https://www.tekkabuzz.win/cricket-betting" heading={t.heading} ctaText={t.ctaText} ctaLink="/go/sports" content={t.content} />; }

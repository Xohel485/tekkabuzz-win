import { useLocale } from "@/hooks/useLocale"; import { GAME_PAGES } from "@/lib/gameTranslations"; import GameInfoPage from "@/components/GameInfoPage";
export default function Mines() { const locale = useLocale(); const t = GAME_PAGES.mines[locale]; return <GameInfoPage title={t.title} description={t.description} keywords="tekkabuzz mines, মাইনস গেম" canonical="https://www.tekkabuzz.win/mines" heading={t.heading} ctaText={t.ctaText} ctaLink="/go/crash" content={t.content} />; }
